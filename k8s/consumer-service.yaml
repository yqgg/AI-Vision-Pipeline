apiVersion: v1
kind: Service
metadata:
  name: vision-consumer-service
spec:
  selector:
    app: vision-consumer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP